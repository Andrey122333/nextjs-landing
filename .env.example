# Next.js Environment Variables

# Framework version (для отображения в футере)
NEXT_PUBLIC_FRAMEWORK_VERSION=15.1.0

# Port (по умолчанию 3000)
PORT=3000

# Node Environment
NODE_ENV=production

# API URLs (если потребуется в будущем)
# NEXT_PUBLIC_API_URL=http://localhost:4000
